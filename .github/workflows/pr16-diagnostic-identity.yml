name: PR16 Baseline Raw Diagnostic Smoke

on:
  push:
    branches:
      - diag/pr16-identity-audit
    paths:
      - .github/workflows/pr16-diagnostic-identity.yml

permissions:
  contents: read

jobs:
  baseline-raw-diagnostic-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      BASE_SHA: 9c24143327abfb2bdd9a6a9b0d13cd2f1422eea1
      PR_HEAD: b5b56a691cb5948bcf3c4ccfbcd431d746509073
      FEATURE_BRANCH: feature/v065-production-round-start
      GDEVELOP_SHA256: c1d23616562befbde242412913785dacb0bb15a79e8c101c1553dc729b1e0472
      WORKFLOW_PATH: .github/workflows/pr16-diagnostic-identity.yml
      RUNTIME_BUNDLE: www/static/js/local-app.4119ac3d.chunk.js
      SENTINEL: __IW_DIAG_JSON__
    steps:
      - name: Checkout diagnostic branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Initial no-drift guard
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main "$FEATURE_BRANCH"
          test "$(git rev-parse origin/main)" = "$BASE_SHA"
          test "$(git rev-parse origin/$FEATURE_BRANCH)" = "$PR_HEAD"
          test "$(git diff --name-only "$PR_HEAD"..HEAD)" = "$WORKFLOW_PATH"
          echo 'initial guard PASS'

      - name: Prepare exact GDevelop and current-main baseline
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update >/dev/null
          sudo apt-get install -y libnss3 libasound2t64 xvfb unzip curl >/dev/null
          curl -L --fail --retry 3 -o /tmp/gdevelop.zip "https://github.com/4ian/GDevelop/releases/download/v5.6.282/GDevelop-5-5.6.282-linux.zip" >/dev/null
          echo "$GDEVELOP_SHA256  /tmp/gdevelop.zip" | sha256sum -c -
          rm -rf /tmp/gdevelop /tmp/gdevelop-app /tmp/report /tmp/strict
          mkdir -p /tmp/gdevelop /tmp/report /tmp/strict
          unzip -q /tmp/gdevelop.zip -d /tmp/gdevelop
          GDEVELOP_BIN="$(find /tmp/gdevelop -type f -name gdevelop -perm -111 | head -n 1)"
          ASAR="$(find /tmp/gdevelop -type f -name app.asar | head -n 1)"
          test -n "$GDEVELOP_BIN" && test -n "$ASAR"
          echo "GDEVELOP_BIN=$GDEVELOP_BIN" >> "$GITHUB_ENV"
          echo "ASAR=$ASAR" >> "$GITHUB_ENV"
          git show "$BASE_SHA:game.json" > /tmp/baseline-game.json
          python3 -m json.tool /tmp/baseline-game.json >/dev/null
          echo 'exact GDevelop + baseline PASS'

      - name: Instrument temporary runtime bundle for raw diagnostic JSON
        shell: bash
        run: |
          set -euo pipefail
          npx --yes asar@3.2.0 extract "$ASAR" /tmp/gdevelop-app >/dev/null
          test -f "/tmp/gdevelop-app/$RUNTIME_BUNDLE"
          node <<'NODE'
          const fs = require('fs');
          const path = require('path');
          const p = path.join('/tmp/gdevelop-app', process.env.RUNTIME_BUNDLE);
          const text = fs.readFileSync(p, 'utf8');
          const anchor = 'const t=(0,No.d8)(e);if(t.length>0)throw console.error(';
          const count = text.split(anchor).length - 1;
          if (count !== 1) throw new Error(`instrumentation anchor count=${count}`);
          const replacement = 'const t=(0,No.d8)(e);console.error("' + process.env.SENTINEL + '"+JSON.stringify(t));if(t.length>0)throw console.error(';
          fs.writeFileSync(p, text.replace(anchor, replacement));
          console.log('temporary instrumentation anchor PASS');
          NODE
          npx --yes asar@3.2.0 pack /tmp/gdevelop-app /tmp/app.instrumented.asar >/dev/null
          cp /tmp/app.instrumented.asar "$ASAR"
          echo 'temporary app.asar repack PASS'

      - name: Run one baseline strict diagnostic and extract JSON
        shell: bash
        run: |
          set -euo pipefail
          set +e
          (cd /tmp/strict && xvfb-run -a "$GDEVELOP_BIN" --no-sandbox --disable-update-check --block-on-diagnostic-errors --run-command EXPORT_HTML5_EXTERNAL /tmp/baseline-game.json) > /tmp/baseline-strict.log 2>&1
          rc=$?
          set -e
          test "$rc" -ne 0
          python3 <<'PY'
          import json
          from pathlib import Path
          sentinel='__IW_DIAG_JSON__'
          lines=Path('/tmp/baseline-strict.log').read_text(encoding='utf-8',errors='replace').splitlines()
          hits=[]
          for line in lines:
              if sentinel in line:
                  payload=line.split(sentinel,1)[1].strip()
                  try:
                      value=json.loads(payload)
                      hits.append(value)
                  except Exception:
                      pass
          if len(hits)!=1:
              raise SystemExit(f'valid sentinel JSON hit count={len(hits)}')
          data=hits[0]
          if not isinstance(data,list):
              raise SystemExit('diagnostic payload is not an array')
          Path('/tmp/report/baseline-diagnostics.json').write_text(json.dumps(data,ensure_ascii=False,indent=2),encoding='utf-8')
          first=data[0] if data else None
          if first is not None and not isinstance(first,dict):
              raise SystemExit('first diagnostic is not an object')
          keys=sorted(first.keys()) if isinstance(first,dict) else []
          expected={'type','isCondition','instructionType','instructionSentence','locationName','locationType','eventPath'}
          schema_ok=(first is None) or expected.issubset(first.keys())
          summary='\n'.join([
              'GDevelop=5.6.282',
              'baseline=current-main-equivalent',
              'baselineSHA=9c24143327abfb2bdd9a6a9b0d13cd2f1422eea1',
              f'diagnosticCount={len(data)}',
              'jsonParse=PASS',
              f'firstSchema={"PASS" if schema_ok else "PARTIAL"}',
              f'firstKeys={json.dumps(keys,ensure_ascii=False)}',
              'verdict=GO' if schema_ok else 'verdict=STOP',
              ''
          ])
          Path('/tmp/report/baseline-summary.txt').write_text(summary,encoding='utf-8')
          print(summary)
          if not schema_ok:
              raise SystemExit('first diagnostic schema did not contain expected core fields')
          PY
          (cd /tmp/report && sha256sum baseline-diagnostics.json baseline-summary.txt > MANIFEST.sha256)

      - name: Upload minimal baseline diagnostic artifact
        uses: actions/upload-artifact@v4
        with:
          name: icewolf-pr16-baseline-diagnostics
          path: |
            /tmp/report/baseline-diagnostics.json
            /tmp/report/baseline-summary.txt
            /tmp/report/MANIFEST.sha256
          if-no-files-found: error
          retention-days: 7
          compression-level: 9

      - name: Final no-drift guard
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main "$FEATURE_BRANCH"
          test "$(git rev-parse origin/main)" = "$BASE_SHA"
          test "$(git rev-parse origin/$FEATURE_BRANCH)" = "$PR_HEAD"
          test "$(git diff --name-only "$PR_HEAD"..HEAD)" = "$WORKFLOW_PATH"
          echo 'final guard PASS'
